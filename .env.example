# Neon Postgres. Populated automatically by the Vercel Neon integration;
# for local dev, run `vercel env pull .env` after that's set up, or paste
# the pooled/direct connection strings from the Neon dashboard.
DATABASE_URL=""
DIRECT_URL=""

# bcrypt hash of your chosen admin password. Generate with:
# node -e "console.log(require('bcryptjs').hashSync('YOUR_PASSWORD', 10))"
ADMIN_PASSWORD_HASH=""

# Random 32-byte hex secret used to sign the admin session JWT. Generate with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=""
